#pragma once

// Copy this file to WatchConfig.h, then replace both placeholder strings.
// WatchConfig.h is ignored by Git so credentials are not published by accident.
#define WATCH_WIFI_SSID "YOUR_WIFI_NAME"
#define WATCH_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
